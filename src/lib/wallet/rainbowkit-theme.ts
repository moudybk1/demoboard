import { darkTheme } from "@rainbow-me/rainbowkit";

/** RainbowKit modal tokens aligned with BOARD's chocolate + gold tabletop. */
export const boardRainbowTheme = darkTheme({
  accentColor: "#ffd23a",
  accentColorForeground: "#2d170c",
  borderRadius: "medium",
  fontStack: "rounded",
  overlayBlur: "small",
});
